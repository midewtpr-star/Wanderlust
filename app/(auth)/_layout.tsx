import { Stack } from "expo-router";

// Auth flow (phone-first, decisions.md D4). Placeholder screens only in the scaffold.
export default function AuthLayout() {
  return <Stack screenOptions={{ headerShown: true }} />;
}
