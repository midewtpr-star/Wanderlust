import { Screen } from "@/components/screen";

// Create a trip (placeholder — build-plan Phase 2).
export default function CreateScreen() {
  return (
    <Screen
      title="Create a trip"
      subtitle="Title, cover, destination, dates, car rental, and the Airbnb-selection stub go here. Placeholder — build-plan Phase 2."
    />
  );
}
