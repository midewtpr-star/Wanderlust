// Single import site for the color scheme hook. NativeWind's hook stays in sync
// with the `dark` class that drives the CSS-variable theme in global.css.
export { useColorScheme } from "nativewind";
