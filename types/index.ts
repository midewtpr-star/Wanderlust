// AppName — shared TypeScript types.
// Placeholder for the scaffold. Domain types (Trip, TripMember, TravelProof,
// MoneyPool, Activity, ...) will be added here or generated from Supabase in later
// phases — see docs/data-model.md.

export type ID = string;
